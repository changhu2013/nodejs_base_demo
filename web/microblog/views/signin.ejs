<!DOCTYPE html>
<html>
  <head>
    <title><%=locals.appName%></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <link rel="stylesheet" href="/bootstrap-3.2.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="/javascripts/jquery-1.11.1.min.js"></script>
    <script src="/bootstrap-3.2.0/js/bootstrap.min.js"></script>
  </head>
  <body>
    <div class="container">
    <%if(locals.error && locals.error.length > 0){%>
      <div class="alert alert-danger"><%=locals.error%></div>
    <%}%>
    <form role="form" method="POST">
      <div class="text-center"><h1><%=locals.appName%></h1></div>
      <div class="form-group">
        <label for="userCode">账户</label>
        <input type="text" class="form-control" 
          name="userCode" id="userCode" 
          placeholder="请输入账户" value="<%=locals.userCode%>">
      </div>
      <div class="form-group">
        <label for="password">密码</label>
        <input type="password" class="form-control" 
          name="password-repeat" id="password" 
          placeholder="请输入密码" value="<%=locals.password%>">
      </div>
      <div class="form-group">
        <label for="password">重输密码</label>
        <input type="password" class="form-control" name="password" id="password" placeholder="请再次输入密码">
      </div>
      <button type="submit" class="btn btn-primary btn-lg btn-block">注册</button>
    </form>
    </div>
  </body>
</html>
